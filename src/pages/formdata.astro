---
import Checkboxes from '../components/inputs/Checkboxes.astro';

const errors = { username: "", email: "", password: "",  };

if (Astro.request.method === "POST") {
  try {
    const data = await Astro.request.formData();
    const name = data.get("username");
    const email = data.get("email");
    const password = data.get("password");

    const checkboxes = {
      one: data.get("one"),
      two: data.get("two"),
      three: data.get("three"),
    } 
  console.log({name, email, password, checkboxes});
  } catch (error) {
    if (error instanceof Error) {
      console.error(error.message);
    }
  }
}

---
<h1>Register</h1>
<form method="POST">
  <label>
    Username:
    <input type="text" name="username" value="lol"/>
  </label>
  {errors.username && <p>{errors.username}</p>}
  <label>
    Email:
    <input type="email" name="email" value="lol@lol.lol" required />
  </label>
  {errors.email && <p>{errors.email}</p>}
  <label>
    Password:
    <input type="password" name="password" value="passuwordy" required minlength="6" />
  </label>
  {errors.password && <p>{errors.password}</p>}
  <Checkboxes />
  <button>Register</button>
</form>